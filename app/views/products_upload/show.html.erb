<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Detalles del Producto</title>
  <style>
    /* Estilos generales */
    body {
      font-family: Arial, sans-serif;
      background-color: #f0f4f8; /* Fondo ligeramente más claro */
      display: flex;
      justify-content: center;
      align-items: center;
      padding: 30px;
    }

    h1 {
      color: #2c3e50; /* Tono azul oscuro más elegante */
      font-size: 28px;
      text-align: center;
      margin-bottom: 20px;
      position: relative;
    }

    h1::after {
      content: "";
      width: 80px;
      height: 3px;
      background-color: #2980b9; /* Azul moderado */
      position: absolute;
      bottom: -5px;
      left: 50%;
      transform: translateX(-50%);
      border-radius: 3px;
    }

    /* Contenedor principal */
    .product-details-container {
      display: flex;
      background-color: #ffffff;
      border-radius: 12px;
      box-shadow: 0 6px 15px rgba(0, 0, 0, 0.1); /* Sombra más suave */
      overflow: hidden;
      width: 80%;
      max-width: 1000px;
      transition: transform 0.3s;
    }

    .product-details-container:hover {
      transform: translateY(-5px); /* Pequeño efecto de elevación */
      box-shadow: 0 10px 20px rgba(0, 0, 0, 0.15); /* Sombra al hacer hover */
    }

    /* Contenedor de la imagen */
    .product-image-container {
      width: 45%;
      background-color: #ecf0f1; /* Fondo claro para contraste */
      display: flex;
      justify-content: center;
      align-items: center;
      padding: 20px;
      border-right: 2px solid #dee2e6;
    }

    .product-image-container img {
      max-width: 100%;
      max-height: 250px;
      border-radius: 8px;
      box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
    }

    .image-placeholder {
      width: 200px;
      height: 200px;
      background-color: #bdc3c7;
      border-radius: 8px;
      display: flex;
      justify-content: center;
      align-items: center;
      font-size: 16px;
      color: #7f8c8d;
    }

    /* Contenedor de los detalles */
    .product-info-container {
      width: 55%;
      padding: 30px;
      display: flex;
      flex-direction: column;
      justify-content: space-between;
      color: #34495e;
    }

    .product-info {
      margin-bottom: 20px;
    }

    .product-info p {
      margin: 8px 0;
      font-size: 18px;
      line-height: 1.4;
    }

    .product-info p strong {
      color: #2980b9; /* Tono azul moderado */
    }

    .button {
      display: inline-block;
      text-align: center;
      padding: 12px 25px;
      margin-top: 20px;
      color: #ffffff;
      background-color: #2980b9; /* Azul moderado */
      text-decoration: none;
      border-radius: 8px;
      font-size: 18px;
      font-weight: bold;
      transition: background-color 0.3s, transform 0.3s;
      align-self: flex-start;
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2); /* Sombra suave */
    }

    .button:hover {
      background-color: #1f5e8a; /* Azul un poco más oscuro */
      transform: scale(1.05); /* Efecto de crecimiento al pasar el cursor */
    }
  </style>
</head>
<body>

  <div class="product-details-container">
    <!-- Contenedor de la imagen -->
    <div class="product-image-container">
      <% if @product.image.attached? %>
        <%= image_tag @product.image, alt: @product.name %>
      <% else %>
        <div class="image-placeholder">Sin imagen</div>
      <% end %>
    </div>

    <!-- Contenedor de los detalles -->
    <div class="product-info-container">
      <div class="product-info">
        <h1>Detalles del Producto</h1>
        <p><strong>Nombre:</strong> <%= @product.name %></p>
        <p><strong>Descripción:</strong> <%= @product.description %></p>
        <p><strong>Precio:</strong> S/<%= @product.price %></p>
        <p><strong>Categoría:</strong> <%= @product.category %></p>
        <p><strong>Marca:</strong> <%= @product.brand %></p>
        <p><strong>Stock:</strong> <%= @product.stock %></p>
        <p><strong>Material:</strong> <%= @product.material %></p>
        <p><strong>Dimensión:</strong> <%= @product.dimension %></p>
        <p><strong>Peso:</strong> <%= @product.peso %></p>
        <p><strong>Color:</strong> <%= @product.color %></p>
        <p><strong>Características:</strong> <%= @product.caracteristicas %></p>
      </div>
      <%= link_to 'Subir otro producto', new_products_upload_path, class: 'button' %>
    </div>
  </div>

</body>
</html>
